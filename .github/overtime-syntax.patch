diff --git a/app/Services/OvertimeService.php b/app/Services/OvertimeService.php
--- a/app/Services/OvertimeService.php
+++ b/app/Services/OvertimeService.php
@@ -81,7 +81,7 @@ class OvertimeService
                 'planned_minutes' => $plannedMinutes,
                 'rate_multiplier' => $policy->rate_multiplier,
                 'status' => OvertimeRequest::STATUS_PENDING,
-                'reason' => $data['reason)],
+                'reason' => $data['reason'],
                 'attachment' => $data['attachment'] ?? null,
             ]);
         });
